# zk-exam · 自考大题精练

自考 **02325《计算机系统结构》** 与 **04741《计算机网络原理》** 近三年大题真题与参考答案，
移动端优先，可部署为静态站点。

---

## 内容构成

| 板块 | 内容 | 数量 |
|------|------|------|
| 真题大题 | 202304 / 202310 / 202404 / 202410 / 202504 / 202604 两科主观题 | 108 道 |
| 预测题 | 每科 3 套，按真题结构自拟 | 6 套 / 54 道 |
| 考点地图 | 近3年精确考频 + 全考期热度 + 考点→题目索引 | 7 个模块 × 2 科 |
| 考点速记 | 填空题题干与答案 + 简答背诵库 | 120 空 + 66 问答 |
| 图表 | 答案附图（从参考答案 PDF 无损抽出）+ 原题影像（扫描件裁切） | 16 + 9 张 |

**图片题怎么处理**

题目和答案里的图分两类，处理方式不同：

1. **答案附图** —— 参考答案 PDF 里是**独立嵌入的位图**，可直接无损抽出（如「答26图」状态转移图、「答28表」页面置换表）。
   归属由「图所在页 + 图内容 + 该题答案里的图号引用」三者互证确定，不靠猜。
2. **原题影像** —— 扫描版题目的图**被压在整页扫描图里，没有独立图层**，只能裁切。
   做法：tesseract 词级坐标定位题号（左边距取众数、排除图中数字干扰、页脚不计入正文），
   裁出「本题号 → 下一题号」的区域，跨页题目自动续裁，再从**原始 PDF 600dpi 重渲染**以避免放大发虚。

图片在题目卡里点击可放大，支持双击切换倍率与拖动平移。另有「只看含图的题」筛选（`?fig=1`）。

> 清晰度上限：原始扫描件约 **278dpi**，再提高渲染分辨率不会产生新细节。

**考期覆盖**
- 02325：可用 12 个考期（201910、202010、202104、202110、202204、202210、202304、202310、202404、202410、202504、202604）
- 04741：可用 18 个考期（201704 – 202604）

**已知缺口（如实标注，未编造）**
- `04741-202410` 原卷组委会未收录题目文件，仅存参考答案 → 该考期 9 道大题有答案无题干。
- 02325 的 201704–201904、202004 六份参考答案为纯扫描件，未纳入考频统计。

---

## 技术栈

- Vite 6 + Vue 3（`<script setup>`）+ vue-router（history 模式）
- 样式：原生 CSS + 设计 token（`src/styles/tokens.css`），**不引 UI 框架、不用 Tailwind**
- 无状态管理库（数据静态，composable 足够）
- 数据为构建期打包的 JSON（`src/data/*.json`）
- 自评进度存 `localStorage`，不联网、不上传

## 目录结构

```
src/
  data/          构建期数据（真题 / 填空 / 考频 / 问答 / 预测题；题目含 answerFigures、stemFigures 字段）
  lib/data.js    数据访问、文本渲染、掌握度存储、搜索索引
  components/    QCard.vue（题目卡：题头 / 原题影像 / 展开答案 / 答案附图 / 涂卡自评 / 原卷链接）
                 Lightbox.vue（图片放大：真实宽度缩放 + 平移 + 双击切换倍率）
  views/         Home / Questions / Predict / Topics / Cards / Search
  styles/        tokens.css（设计变量）· base.css（基础与共享组件）
public/
  papers/        原卷 PDF（23 份，约 18MB）
  figs/          图片资产（25 张，约 1.9MB）：`*-ans.*` 答案附图、`*-stem.jpg` 原题影像
  manifest.json  favicon.svg
```

## 开发与构建

```bash
npm install
npm run dev       # 局域网可访问，端口 5175
npm run build     # 产物在 dist/
npm run preview   # 预览构建产物，端口 4173
```

## 部署

`vercel.json` 已配置 SPA 回退（除 `papers/` 与 `assets/` 外全部重写到 `index.html`），
`papers/` 加了长缓存头。推到 GitHub 后由 Vercel 导入即可，无需额外配置。

---

## 设计说明

视觉方向取自题材本身——试卷与答题卡的物质语言，而不是通用落地页模板：

- **色彩**：墨蓝黑 `#141C26`（试卷油墨）／冷白纸 `#F7F7F4`／批改红 `#C8342B`／密封线靛 `#3A5A8C`／铅笔灰 `#61686F`
- **字体**：标题与题头用中文衬线（Songti SC / SimSun / Noto Serif CJK SC，走系统字体栈，不引 CDN 字体）；正文用系统黑体；考期、分值、题号用等宽
- **签名元素**：首页左侧**竖排密封线**（`密封线内不要答题`）；掌握度自评做成**答题卡涂格**（涂满＝熟／半涂＝模糊／空框红叉＝不会）；考期索引是**答题卡式密表**
- **印章**只用在「预测题」页——圆形「模拟卷 · 非真题」章，语义正确（标记非官方）
- **可访问性**：正文对比度达 WCAG AA；保留键盘焦点样式；`prefers-reduced-motion` 下关闭动效；语义化标签与 `aria-pressed`

## 数据来源与免责

- 真题题干与参考答案均来自历年真题 PDF；其中 8 份扫描题目经视觉转录、3 份字体乱码答案经视觉还原，并做逐题校对。
- **预测题为自拟模拟题，不是真题**，页面已标注。
- 原卷 PDF 已随站点托管，题目卡提供「看原卷」入口，可逐字核对。
